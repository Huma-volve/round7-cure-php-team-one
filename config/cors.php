<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Paths
    |--------------------------------------------------------------------------
    |
    | المسارات اللي هيسمح Laravel بالـ CORS عليها.
    | هنا بنسمح على كل الـ API و CSRF cookie.
    |
    */
    'paths' => ['api/*', 'sanctum/csrf-cookie'],

    /*
    |--------------------------------------------------------------------------
    | Allowed Methods
    |--------------------------------------------------------------------------
    |
    | السماح بأي HTTP method.
    |
    */
    'allowed_methods' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Allowed Origins
    |--------------------------------------------------------------------------
    |
    | البورت اللي شغال عليه React app.
    | لو React على localhost:5173:
    |
    */
    // للسماح بأي دومين (ويب أو موبايل)
    // ملاحظة: مع wildcard لا تستخدم الكوكيز (supports_credentials يجب أن تكون false)
    'allowed_origins' => [
        'https://round7-cure-react.huma-volve.com',
        'capacitor://localhost',                    
        'http://localhost',                        
        'http://127.0.0.1',
        'https://round7-cure.huma-volve.com',
        '*'
    ],
    // إن كنت بحاجة للكوكيز/المصادقة عبر المتصفح، بدلاً من هذا استخدم:
    // 'allowed_origins' => [],
    // 'allowed_origin_patterns' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Allowed Headers
    |--------------------------------------------------------------------------
    |
    | السماح بأي header.
    |
    */
    'allowed_headers' => ['*'],

    /*
    |--------------------------------------------------------------------------
    | Exposed Headers
    |--------------------------------------------------------------------------
    */
    'exposed_headers' => [],

    /*
    |--------------------------------------------------------------------------
    | Supports Credentials
    |--------------------------------------------------------------------------
    |
    | مهم جداً مع Sanctum علشان يسمح بالـ cookies.
    |
    */
    // عند السماح بأي Origin، عطّل الكريدنشلز
    'supports_credentials' => false,

];
